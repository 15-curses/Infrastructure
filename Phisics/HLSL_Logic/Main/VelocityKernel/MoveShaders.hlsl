/// ============================================================================
/// ГАЙД: КАК ДОБАВИТЬ НОВЫЙ ТИП ДВИЖЕНИЯ (Например: Orbit)
/// ============================================================================
/// 1. Создайте функцию обработки (строго ВЫШЕ инклудов шаблона):
///    void OrbitMoveMetod(uint element) { /* Логика */ }
///
/// 2. В секции генерации ядер добавьте три строки:
///    #pragma kernel Orbit
///    #define CUR_NAME Orbit
///    #include "MoveTemplate.hlsl"
///
/// 3. На стороне CPU (C#) автоматически станут доступны переменные:
///    - OrbitMoveBufferCounter  (атомарный счетчик, обнулять перед Dispatch)
///    - OrbitMoveCount          (общее количество элементов)
///    - OrbitMoveStartID        (смещение в moveBuffer)
///
/// Потоков в группе: всегда 32 (оптимально для 1 варпа NVIDIA).
/// При вызове Dispatch рассчитывайте группы как: (Count + 31) / 32.
/// ============================================================================

#pragma kernel OrbitMove
#define CUR_NAME Orbit
#include "MoveTemplate.hlsl"

